 <h1>New Advertisement</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for advertisements</p>
     <ul>
       <li>Make sure you advertise something original no copycats.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @advertisement do |a| %>
       <div class="form-group">
 
         <%= a.label :title %>

         <%= a.text_field :title, class: 'form-control', placeholder: "Enter advertisement title" %>
       </div>
       <div class="form-group">
         <%= a.label :body %>
         <%= a.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter advertisement body" %>
       </div>
       <div class="form-group">

         <%= a.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>